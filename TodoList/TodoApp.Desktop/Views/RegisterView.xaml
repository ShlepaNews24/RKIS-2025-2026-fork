<UserControl x:Class="TodoApp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
        <TextBlock Text="Регистрация" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="Логин" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="Пароль" Margin="0,0,0,2"/>
        <PasswordBox x:Name="PasswordBox" 
                     PasswordChanged="PasswordBox_PasswordChanged" 
                     Margin="0,0,0,10"/>

        <TextBlock Text="Имя" Margin="0,0,0,2"/>
        <TextBox Text="{Binding FirstName}" Margin="0,0,0,10"/>

        <TextBlock Text="Фамилия" Margin="0,0,0,2"/>
        <TextBox Text="{Binding LastName}" Margin="0,0,0,10"/>

        <TextBlock Text="Год рождения" Margin="0,0,0,2"/>
        <TextBox Text="{Binding BirthYear, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" 
                   Margin="0,0,0,10"/>

        <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" 
                Margin="0,0,0,5" Height="30"/>

        <TextBlock HorizontalAlignment="Center">
            <Hyperlink Command="{Binding CancelCommand}">Отмена</Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>