$kind: http-request
name: Delete Todo
url: "{{base_url}}/api/todos/{{todo_id}}"
method: DELETE
headers:
  Authorization: Bearer {{auth_token}}
scripts:
  - type: afterResponse
    code: pm.test("Status 204 No Content", () => pm.response.to.have.status(204));
    language: text/javascript
order: 4256137728064099
